---
import { Card } from "@web-noise-cleaner/ui/components/ui/card";
import { FaqSection } from "@/components/faq-section";
import { IssueSection } from "@/components/problems-issues";
import { Footer } from "@/components/layout/footer";
import { ImportantNotice } from "@/components/important-notice";
import { installSteps } from "@/constants";
---

<section class="px-6 py-16 lg:px-8">
  <div class="mx-auto max-w-5xl">
    <h2 class="text-2xl font-bold text-center mb-12">インストール方法</h2>
    <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
      {
        installSteps.map((item) => (
          <Card>
            <div class="px-3">
              <span class="text-xs font-bold text-primary tracking-wider uppercase">
                Step {item.step}
              </span>
              <h3 class="font-semibold text-base mt-2 mb-2">{item.title}</h3>
              <p class="text-xs text-muted-foreground leading-relaxed">
                {item.description}
              </p>
            </div>
          </Card>
        ))
      }
    </div>
    <ImportantNotice />
  </div>

  <section class="px-6 pb-16 lg:px-8 mt-10">
    <div class="mx-auto max-w-xl">
      <div class="relative overflow-hidden rounded-xl shadow-lg ring-1 ring-border/40 bg-muted/10">
        <img
          src={`${import.meta.env.BASE_URL}/hero-2.png`}
          alt="ブラウザの拡張機能管理画面"
          class="w-full h-auto object-contain"
          loading="lazy"
        />
      </div>
      <p class="mt-3 text-xs text-center text-muted-foreground">
        Chrome の拡張機能管理画面 — デベロッパーモードをONにして拡張機能を読み込みます
      </p>
    </div>
  </section>

  <FaqSection client:only="react" />
  <IssueSection client:only="react" />
  <Footer />
</section>
